import { t as CompositeStore } from "../composite-store-DyJc-XRA.js";
import { n as GroupOptions } from "../group-_sK_FAxI.js";
import { Props } from "@ariakit/react-utils";
import { ElementType } from "react";

//#region src/composite/composite-group.d.ts
declare const TagName = "div";
type TagName = typeof TagName;
/**
 * Returns props to create a `CompositeGroup` component.
 * @see https://ariakit.com/components/composite
 * @example
 * ```jsx
 * const store = useCompositeStore();
 * const props = useCompositeGroup({ store });
 * <Composite store={store}>
 *   <Role {...props}>
 *     <CompositeGroupLabel>Label</CompositeGroupLabel>
 *     <CompositeItem>Item 1</CompositeItem>
 *     <CompositeItem>Item 2</CompositeItem>
 *   </Role>
 * </Composite>
 * ```
 */
declare const useCompositeGroup: import("@ariakit/react-utils").Hook<"div", CompositeGroupOptions<"div">>;
/**
 * Renders a group element for composite items. The
 * [`CompositeGroupLabel`](https://ariakit.com/reference/composite-group-label)
 * component can be used inside this component so the `aria-labelledby` prop is
 * properly set on the group element.
 * @see https://ariakit.com/components/composite
 * @example
 * ```jsx {3-7}
 * <CompositeProvider>
 *   <Composite>
 *     <CompositeGroup>
 *       <CompositeGroupLabel>Label</CompositeGroupLabel>
 *       <CompositeItem>Item 1</CompositeItem>
 *       <CompositeItem>Item 2</CompositeItem>
 *     </CompositeGroup>
 *   </Composite>
 * </CompositeProvider>
 * ```
 */
declare const CompositeGroup: (props: CompositeGroupProps) => import("react").ReactElement<unknown, string | import("react").JSXElementConstructor<any>>;
interface CompositeGroupOptions<T extends ElementType = TagName> extends GroupOptions<T> {
  /**
   * Object returned by the
   * [`useCompositeStore`](https://ariakit.com/reference/use-composite-store)
   * hook. If not provided, the closest
   * [`Composite`](https://ariakit.com/reference/composite) or
   * [`CompositeProvider`](https://ariakit.com/reference/composite-provider)
   * components' context will be used.
   */
  store?: CompositeStore;
}
type CompositeGroupProps<T extends ElementType = TagName> = Props<T, CompositeGroupOptions<T>>;
//#endregion
export { CompositeGroup, CompositeGroupOptions, CompositeGroupProps, useCompositeGroup };
//# sourceMappingURL=composite-group.d.ts.map